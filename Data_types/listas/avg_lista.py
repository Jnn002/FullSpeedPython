# Obtener el promedio de una lista
numeros = [10, 20, 10, 30, 10]
# Se suman los elementos de la lista y se divide entre la cantidad de elementos
for x in numeros:
    suma = sum(numeros) / len(numeros)
print(suma)